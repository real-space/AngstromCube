#pragma once

#include "status.hxx" // status_t

namespace structure_solver {
  
//   status_t init(int const echo=0);
//   
//   status_t solve(int const echo=0);

  status_t all_tests(int const echo=0); // declaration only

} // namespace structure_solver
